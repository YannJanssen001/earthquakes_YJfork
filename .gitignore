# Data files
earthquake_data.json          # Downloaded earthquake data - can be regenerated by running the script
earthquake_analysis.png       # Generated plot files - can be regenerated by running analysis
earthquake_analysis_combined.png  # Combined plot file - can be regenerated by running analysis

# Python cache files
__pycache__/                  # Python bytecode cache directory
*.py[cod]                     # Compiled Python files (.pyc, .pyo, .pyd)
*$py.class                    # Jython compiled class files

# Conda environments
.conda/                       # Conda package cache and metadata
conda-meta/                   # Conda environment metadata files
envs/                         # Local conda environments created in project directory

# Environment files
.env                          # Environment variables file (may contain secrets)
.env.local                    # Local environment variables
environment.yml               # Conda environment specification (remove this line if you want to track dependencies)

# IDE files
.vscode/                      # Visual Studio Code settings and workspace files
.idea/                        # JetBrains IDE (PyCharm) project files
*.swp                         # Vim swap files
*.swo                         # Vim swap files (alternative extension)

# Windows files
Thumbs.db                     # Windows thumbnail cache
Desktop.ini                   # Windows folder customization file
*.lnk                         # Windows shortcut files

# macOS files (in case someone uses Mac)
.DS_Store                     # macOS directory metadata file
